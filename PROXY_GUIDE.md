# 🌐 Гайд по использованию прокси

## Зачем нужен прокси?

Прокси помогает:
- ✅ Обойти IP блокировки WD сервера
- ✅ Скрыть реальный IP адрес вашего сервера
- ✅ Распределить нагрузку на несколько IP адресов
- ✅ Работать из страны с блокировками

---

## 🔧 Три варианта использования прокси

### Вариант 1: Простой прокси (без авторизации)

Используйте, если у вас есть доступ к открытому прокси или вашему прокси не требуется пароль.

```env
PROXY_LIST=http://proxy.example.com:8080
```

Или несколько прокси:
```env
PROXY_LIST=http://proxy1.example.com:8080,http://proxy2.example.com:8080
```

---

### Вариант 2: Прокси с авторизацией (username:password)

**Это самый распространённый формат для платных прокси-сервисов.**

```env
PROXY_LIST=http://username:password@proxy.example.com:8080
```

#### Пример с платным сервисом:
```env
# Если вы купили прокси на proxy-provider.com
# Данные: IP=proxy.provider.com, Port=8080, User=john123, Password=secret123
PROXY_LIST=http://john123:secret123@proxy.provider.com:8080
```

#### Несколько прокси с авторизацией:
```env
PROXY_LIST=http://user1:pass1@proxy1.com:8080,http://user2:pass2@proxy2.com:8080
```

---

### Вариант 3: Смешанные прокси

Некоторые с авторизацией, некоторые без:

```env
PROXY_LIST=http://proxy1.com:8080,http://user:pass@proxy2.com:8080,http://proxy3.com:3128
```

---

## 🔐 Безопасность

⚠️ **ВАЖНО:** `.env` файл содержит пароли!

### Как защитить:

```bash
# 1. Убедитесь что .env в .gitignore ✓ (уже добавлено)

# 2. На сервере - ограничьте доступ к файлу
chmod 600 .env

# 3. В Docker - используйте volume с ограничениями
docker-compose up -d  # .env автоматически не попадет в image

# 4. При перезагрузке сервера - зашифруйте .env
```

---

## 📝 Примеры настроек для популярных сервисов

### 1. Smartproxy

```env
# Купили на smartproxy.com
# Получили: страна=Ukraine, proxy=gate.smartproxy.com:7000, user=user-country-ua, pass=pass123

PROXY_LIST=http://user-country-ua:pass123@gate.smartproxy.com:7000
```

### 2. Bright Data (бывший Luminati)

```env
# Customer ID: 12345, Zone: residential, Password: mypass
PROXY_LIST=http://lum-customer-12345-zone-residential:mypass@zproxy.luminati.io:22225
```

### 3. Oxylabs

```env
# Username: user-abc123, Password: pass456, Country filter: UA
PROXY_LIST=http://user-abc123:pass456@pr.oxylabs.io:7777
```

### 4. ScraperAPI

```env
# API Key: scraperapi_abc123def456
PROXY_LIST=http://scraperuser:scraperapi_abc123def456@proxy.scraperapi.com:8001
```

### 5. Proxy-seller / Proxy.ru

```env
# Получили IP:PORT и логин:пароль
PROXY_LIST=http://user:password@proxy.seller.com:9090
```

### 6. Несколько провайдеров одновременно

```env
# Распределяем нагрузку между разными провайдерами
PROXY_LIST=http://smartproxy_user:pass@gate.smartproxy.com:7000,http://bright_user:pass@zproxy.luminati.io:22225,http://oxy_user:pass@pr.oxylabs.io:7777
```

---

## ✅ Как проверить что прокси работает?

### Вариант 1: Локально (перед запуском в Docker)

```bash
# Тестируем прокси
python -c "
import requests
proxies = {
    'http': 'http://user:pass@proxy.example.com:8080',
    'https': 'http://user:pass@proxy.example.com:8080'
}
try:
    response = requests.get('http://ipinfo.io/ip', proxies=proxies, timeout=5)
    print('✅ Прокси работает! Ваш IP:', response.text)
except Exception as e:
    print('❌ Ошибка:', e)
"
```

### Вариант 2: В Docker контейнере

```bash
# Запустите контейнер и проверьте логи
docker-compose up -d
docker-compose logs -f

# Ищите в логах:
# "Proxy with auth configured: proxy.example.com:8080"
# или
# "Proxy configured: http://..."
```

### Вариант 3: Визуально - когда приложение работает

Если приложение правильно подключается к WD и:
- ✅ Отправляет уведомления
- ✅ Нет 503 ошибок
- ✅ Нет timeout ошибок

Значит прокси работает!

---

## 🐛 Типичные проблемы и решения

### Проблема #1: "Connection refused"

```
❌ ERROR: ConnectionError: Connection refused
```

**Причина:** Неправильный IP:PORT прокси

**Решение:**
```bash
# Проверьте что IP:PORT доступны
ping proxy.example.com
telnet proxy.example.com 8080

# Если не работает - проверьте:
# 1. IP адрес правильный?
# 2. Port правильный?
# 3. Прокси включен и работает?
```

### Проблема #2: "407 Proxy Authentication Required"

```
❌ ERROR: HTTP 407 - Authentication required
```

**Причина:** Неправильный username или password

**Решение:**
```bash
# Проверьте в .env:
# PROXY_LIST=http://USERNAME:PASSWORD@proxy.example.com:PORT
#            ✅ username          ✅ password

# Убедитесь что:
# 1. Username не пустой
# 2. Password не пустой
# 3. Нет ошибок в логине/пароле
# 4. Если есть спецсимволы - экранируйте их!
```

**Спецсимволы в пароле:**
Если ваш пароль содержит `@`, `:`, `#` и т.д., используйте URL encoding:

```python
from urllib.parse import quote

password = "pass@word:123"
encoded_password = quote(password, safe='')  # "pass%40word%3A123"

# В .env:
# PROXY_LIST=http://user:pass%40word%3A123@proxy.example.com:8080
```

### Проблема #3: "Connection timeout"

```
❌ ERROR: Timeout error: ('Connection aborted', RemoteDisconnected())
```

**Причина:** Прокси медленный или перегруженный

**Решение:**
```bash
# 1. Увеличьте timeout в .env
REQUEST_TIMEOUT=30  # вместо 10

# 2. Добавьте еще прокси в PROXY_LIST - система автоматически переключится

# 3. Выберите прокси в другой стране (ближе к WD серверу)
```

### Проблема #4: "503 Service Unavailable" не решается прокси

```
❌ WARNING: Got 503 from WD, trying with proxy...
❌ ERROR: Failed after trying with proxy
```

**Причина:**
- WD заблокировал и саму прокси сеть
- Или все ваши прокси из одной сети

**Решение:**
```bash
# 1. Используйте прокси от разных провайдеров
PROXY_LIST=http://user:pass@smartproxy.com:7000,http://user:pass@bright.io:22225

# 2. Меняйте User-Agent (добавьте случайность)
# 3. Увеличьте REQUEST_DELAY до 5-10 секунд
REQUEST_DELAY=10

# 4. Используйте residential прокси (более стабильные)
# Выбирайте "Residential" в настройках вашего прокси-провайдера
```

---

## 📊 Сравнение типов прокси

| Тип | Скорость | Блокировки | Цена | Когда использовать |
|-----|----------|-----------|------|-------------------|
| **Free прокси** | Медленно ❌ | Часто блокируют ❌ | Бесплатно | Никогда для production |
| **Shared** | Нормально ⚠️ | Иногда ⚠️ | Дешево | Тестирование |
| **Dedicated** | Быстро ✅ | Редко ✅ | Дороже | Production важный |
| **Residential** | Среднее ⚠️ | Почти никогда ✅ | Дорого | Когда нужна 100% надежность |

---

## 💡 Рекомендации

### Для надежной работы:

1. **Используйте минимум 3-5 прокси** одновременно
   ```env
   PROXY_LIST=http://user:pass@proxy1:port,http://user:pass@proxy2:port,http://user:pass@proxy3:port
   ```

2. **Выбирайте residential прокси** (более надежны для WD)

3. **Из разных провайдеров** (если одна сеть заблокируется, другие работают)

4. **Увеличьте REQUEST_DELAY**
   ```env
   REQUEST_DELAY=5  # вместо 2
   ```

5. **Мониторьте логи**
   ```bash
   docker-compose logs -f | grep -i proxy
   ```

---

## 🔗 Полезные ссылки

- [Smartproxy](https://smartproxy.com) - Residential прокси, хорошее соотношение цена/качество
- [Bright Data](https://brightdata.com) - Самый надежный, но дорогой
- [Oxylabs](https://oxylabs.io) - Хороший выбор для HTTP запросов
- [ScraperAPI](https://www.scraperapi.com) - Специально для веб-скрейпинга

---

## 📋 Чек-лист перед использованием прокси

- [ ] Купили или получили доступ к прокси
- [ ] Знаете IP адрес прокси
- [ ] Знаете PORT прокси
- [ ] Знаете USERNAME (если требуется авторизация)
- [ ] Знаете PASSWORD (если требуется авторизация)
- [ ] Протестировали прокси локально
- [ ] Добавили в .env в правильном формате
- [ ] Перестроили Docker образ: `docker-compose build --no-cache`
- [ ] Запустили контейнер: `docker-compose up -d`
- [ ] Проверили логи что прокси загрузился: `docker-compose logs | grep -i proxy`

---

## 🎉 Все готово!

Приложение автоматически будет:
- ✅ Использовать прокси когда WD вернет 503
- ✅ Переключаться на следующий прокси при ошибке
- ✅ Логировать какой прокси используется
- ✅ Отправлять ошибки в Telegram если все прокси не работают
