# 🚗 Добро пожаловать в WD Block Notificator!

Если вы новичок в этом проекте, прочитайте этот файл в следующем порядке:

## 📚 Порядок изучения

### 1️⃣ **Начните с визуальной диаграммы** (5 минут)
Откройте [FLOW_DIAGRAM.txt](FLOW_DIAGRAM.txt) - там показано как работает приложение в виде ASCII схемы.

👉 **Это самый быстрый способ понять логику!**

---

### 2️⃣ **Прочитайте подробное объяснение** (15 минут)
Откройте [LOGIC_EXPLAINED.md](LOGIC_EXPLAINED.md) - полное объяснение на русском языке, написанное для людей, далёких от IT.

✅ Включает:
- Аналогия с охранником в торговом центре
- Пошаговое объяснение каждого этапа
- Типичная таблица работы в течение дня
- Как приложение защищается от блокировок
- Часто задаваемые вопросы

---

### 3️⃣ **Техническая документация для разработчиков** (при необходимости)
Откройте [CLAUDE.md](CLAUDE.md) - полная техническая документация для программистов.

📋 Включает:
- Архитектура приложения
- Как запустить и тестировать
- API функций
- Обработка ошибок
- Безопасность

---

## 🚀 Быстрый старт (для администраторов)

### Вариант A: Локальный запуск (простейший)

#### Шаг 1: Подготовка
```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env с вашими значениями
```

#### Шаг 2: Установка зависимостей
```bash
pip install -r requirements.txt
```

#### Шаг 3: Запуск
```bash
python main.py
# Приложение будет проверять машины каждый час в 09:10-20:30
# Нажимте Ctrl+C для остановки
```

---

### Вариант B: Docker запуск (рекомендуется для production)

#### Шаг 1: Создайте .env файл
```bash
cp .env.example .env
# Отредактируйте .env с вашими значениями
```

#### Шаг 2: Пересоберите и запустите

**Linux / Mac:**
```bash
chmod +x docker-restart.sh
./docker-restart.sh
```

**Windows:**
```cmd
docker-restart.bat
```

**Или вручную:**
```bash
docker-compose down
docker rmi wd_block_notificator_app
docker-compose build --no-cache
docker-compose up -d
docker-compose logs -f
```

📚 **Подробнее:** см. файл [DOCKER_GUIDE.md](DOCKER_GUIDE.md)

---

## ⚙️ Конфигурация (.env файл)

Вот что нужно заполнить в `.env`:

### Telegram боты (обязательно!)
```env
# Основной бот - отправляет сообщения о заблокированных машинах
TELEGRAM_BOT_TOKEN=5005136355:AAE8e8rNV71_7d1MXuNw4eR3GWY2xgjWmr8

# Бот ошибок - отправляет критические ошибки администратору
TELEGRAM_ERROR_BOT_TOKEN=1014138811:AAFthY3Sks79L04jcYrQt7gLeKeH3foOTio
TELEGRAM_ERROR_CHAT_ID=-353220657
```

### WD диспетчерская (обязательно!)
```env
WD_LOGIN=fly
WD_PASSWORD=0933137532
```

### Данные таксикомпаний (обязательно!)
```env
# Повторите для каждой таксикомпании: FLY, JET, MAGDACK, TAXI898, ALLO
FLY_HOST=10.0.15.5
FLY_DB=C:/taxi/DB/TAXI.GDB
FLY_NAME=Флай
FLY_CHAT_ID=-1002045607452  # Где отправлять уведомления о Fly
```

### Опциональные параметры
```env
# Sentry (для отслеживания ошибок на сайте sentry.io)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Прокси (для обхода блокировок при 503)
PROXY_LIST=http://proxy1:8080,http://proxy2:8080

# Насколько часто отправлять запросы (в секундах)
REQUEST_DELAY=2

# Таймаут подключения (в секундах)
REQUEST_TIMEOUT=10

# Количество попыток переподключения
REQUEST_RETRIES=3

# Debug режим
DEBUG=False
```

---

## 📊 Мониторинг работы

### Где смотреть логи?

**Файлы логов** (в папке проекта):
- `Fly.log` - логи по Fly такси
- `Jet.log` - логи по Jet такси
- `Magdack.log` - логи по Magdack такси
- и т.д.

**Telegram канал ошибок** (TELEGRAM_ERROR_CHAT_ID):
- Критические ошибки приложения
- Проблемы с подключением
- Предупреждения (503, таймауты)

**Sentry dashboard** (если настроен):
- https://sentry.io → ваш проект
- Централизованный просмотр всех ошибок

---

## ⚡ Основные функции

| Функция | Когда срабатывает | Что происходит |
|---------|-------------------|----------------|
| **Получение чёрного списка** | Каждый час в рабочее время | Запрашиваем список заблокированных машин у WD |
| **Проверка в каждом такси** | Каждый час | Проверяем, есть ли заблокированные машины в наших такси |
| **Отправка уведомления** | Если найдена новая заблокированная машина | Отправляем сообщение в Telegram с информацией о машине |
| **Проверка в полиции** | При отправке уведомления | Ищем информацию о машине на сайте baza-gai.com.ua |
| **Сохранение в БД** | После отправки уведомления | Запоминаем, что эту машину уже сообщали (чтоб не спамить) |
| **Обработка 503** | Если WD вернул "Service Unavailable" | Пробуем через прокси |
| **Отправка ошибки** | При критической ошибке | Отправляем алерт в Telegram и Sentry |

---

## 🐛 Типичные проблемы и решения

### Проблема: "Приложение вообще ничего не отправляет"
**Решение:**
1. Проверьте, в рабочее ли время (09:10-20:30)
2. Посмотрите в логи `Fly.log` и т.д.
3. Проверьте, что `.env` файл заполнен правильно
4. Проверьте интернет соединение

### Проблема: "Отправляются дублирующиеся сообщения"
**Решение:**
- Удалите файл `processed_cars.db` и перезагрузите приложение
- База будет пересоздана автоматически

### Проблема: "WD вернул 503"
**Решение:**
1. Это нормально - приложение автоматически пробует через прокси
2. Если прокси не настроен - добавьте в `.env`:
   ```env
   PROXY_LIST=http://proxy.example.com:8080
   ```

### Проблема: "Не можу подключиться к Firebird БД"
**Решение:**
1. Проверьте, доступен ли сервер Firebird по IP (из `.env`)
2. Проверьте путь к БД файлу (из `.env`)
3. Проверьте логин/пароль в коде (`evos.user = 'SYSDBA'`, `evos.password = 'masterkey'`)

### Проблема: "Приложение крашится"
**Решение:**
1. Посмотрите в консоль (stdout)
2. Посмотрите в лог файлы
3. Проверьте Telegram канал ошибок
4. Проверьте Sentry (если настроен)

---

## 🔐 Безопасность

### ⚠️ Важно!

1. **Никогда не коммитьте `.env` файл!**
   - Он содержит пароли и токены
   - Уже добавлен в `.gitignore`

2. **Держите токены и пароли в секрете**
   - Если кто-то украдет TELEGRAM_BOT_TOKEN - он сможет отправлять сообщения от вас
   - Если кто-то украдет WD_PASSWORD - он получит доступ к WD системе

3. **Ротируйте пароли регулярно**
   - Пароли Telegram (recreate bot in BotFather)
   - WD пароли

---

## 📞 Контакты / Поддержка

Если у вас есть вопросы:
1. Прочитайте [LOGIC_EXPLAINED.md](LOGIC_EXPLAINED.md) - там есть FAQ раздел
2. Посмотрите в [FLOW_DIAGRAM.txt](FLOW_DIAGRAM.txt) - может быть ответ там
3. Проверьте логи (`*.log` файлы)
4. Посмотрите на Sentry (если настроен)

---

## 📖 Дополнительная информация

| Файл | Для кого | Что внутри |
|------|----------|-----------|
| [LOGIC_EXPLAINED.md](LOGIC_EXPLAINED.md) | Для всех (особенно не IT) | Понятное объяснение того, как это работает |
| [FLOW_DIAGRAM.txt](FLOW_DIAGRAM.txt) | Для визуалов | ASCII схемы и диаграммы |
| [CLAUDE.md](CLAUDE.md) | Для разработчиков | Техническая документация |
| [main.py](main.py) | Для программистов | Основной код приложения |
| [utils.py](utils.py) | Для программистов | Вспомогательные функции |

---

## ✅ Чек-лист для администратора

- [ ] Скопирован `.env.example` в `.env`
- [ ] Заполнены все обязательные переменные в `.env`
- [ ] Установлены зависимости: `pip install -r requirements.txt`
- [ ] Протестирован запуск: `python main.py`
- [ ] Проверено, что сообщения приходят в Telegram
- [ ] Добавлена проверка ошибок (опционально: Sentry)
- [ ] Настроены прокси если нужно (опционально)
- [ ] `.env` файл добавлен в `.gitignore` ✓ (уже сделано)

---

**Готово! Теперь ваше приложение работает! 🎉**

Если что-то не работает - посмотрите в логи и прочитайте FAQ в [LOGIC_EXPLAINED.md](LOGIC_EXPLAINED.md).
