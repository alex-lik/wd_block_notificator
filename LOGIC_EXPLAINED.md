# 🚗 Как работает WD Block Notificator

## Аналогия: Охранник в торговом центре

Представьте, что это приложение - это **охранник в торговом центре**, который:
1. **Смотрит список** людей, которым запретили входить (чёрный список)
2. **Проверяет толпу** в центре - есть ли там эти люди
3. **Звонит начальству** если кого-то из чёрного списка нашёл
4. **Следит, что б не было сбоев** в его системе

---

## 🔄 Как это работает в реальности

### ЭТАП 1️⃣: Получение чёрного списка (каждый час)

```
Приложение спрашивает у WD сервера:
"Привет, WD! Дай мне список всех заблокированных машин!"

WD отвечает:
"Вот, заблокированы эти машины:
- AA8732CC - не соответствует требованиям сервера
- AI3955IE - нарушение правил
- KA9628HA - задолженность"
```

**Что происходит в коде:**
```python
def get_black_list(session):
    # Получаем чёрный список от WD
    black_list = check_number_on_block_by_soz(session, 303)
    black_list = check_number_on_block_by_soz(session, 296)
    # Результат: {'AA8732CC': 'причина', 'AI3955IE': 'причина', ...}
```

---

### ЭТАП 2️⃣: Проверка в каждой таксикомпании (каждый час)

Для каждой таксикомпании (Fly, Jet, Magdack, 898, Allo):

```
Приложение подключается к базе данных машин:
"Привет, база данных Fly! Дай мне список всех машин и их водителей!"

БД отвечает:
"Вот, у нас есть эти машины:
- AA8732CC - водитель Иванов Иван, номер 0977353647
- KA9628HA - водитель Петров Петр, номер 0988765432
- BM056CI - водитель Сидоров Сидор, номер 0665554433"
```

**Что происходит в коде:**
```python
for taxi in ['Jet', 'Fly', 'Magdack', '898', 'Allo']:
    # Получаем список машин из базы данных этой таксикомпании
    cars = get_cardata(host, database, taxi_name)
    # Результат: {
    #   'AA8732CC': {'f': 'Иванов', 'i': 'Иван', 'phone1': '0977353647', ...},
    #   'KA9628HA': {'f': 'Петров', 'phone1': '0988765432', ...},
    # }
```

---

### ЭТАП 3️⃣: Поиск пересечений (кто заблокирован?)

Приложение берёт чёрный список и ищет этих машин в каждой таксикомпании:

```
Чёрный список: AA8732CC, AI3955IE, KA9628HA, ...
Машины Fly:   AA8732CC✓, KA9628HA✓, BM056CI, ...

НАШЛИ! В таксикомпании Fly есть 2 заблокированные машины!
```

**Что происходит в коде:**
```python
for carnum in black_list:           # Для каждой заблокированной машины
    if carnum in cars:              # Если её нашли в этой таксикомпании
        # Нашли! Отправляем уведомление
        send_message(message, chat_id)
```

---

### ЭТАП 4️⃣: Сбор информации о машине

Когда нашли заблокированную машину, приложение собирает всю информацию:

```
Информация о машине AA8732CC:
├─ Номер: AA8732CC
├─ Позывной (call sign): 8188
├─ Марка: Daewoo Lanos
├─ Год: 2004
├─ Цвет: синий
├─ Водитель: Сенченко Віталій Олександрович
├─ Телефоны: 0977353647, 0988765432
├─ Баланс счёта: 0 гривен
├─ Последняя активность: 2025-12-27
└─ Проверка в полиции: "Синий ЗАЗ-DAEWOO LANOS 2004"

Причина блокировки: "Авто не відповідає вимогам сервера"
```

**Что происходит в коде:**
```python
data = cars[carnum]  # Получаем все данные машины
contacts = f"{driver_name}\n{phone1}\n{phone2}"  # Собираем контакты
police_info = police.check_in_police(carnum)    # Проверяем в полиции
message = f"Машина {carnum}:\n{contacts}\n{police_info}"  # Составляем сообщение
```

---

### ЭТАП 5️⃣: Отправка уведомления в Telegram

Приложение **отправляет сообщение в группу Telegram**:

```
🚨 МАШИНА ЗАБЛОКИРОВАНА 🚨

AA8732CC - позывной: 8188, марка: Daewoo Lanos, год: 2004, цвет: синій

Сенченко Віталій Олександрович
0977353647
0988765432
Баланс: 0.0
Был в программе: 2025-12-27

По данным сайта baza-gai.com.ua: Синий ЗАЗ-DAEWOO LANOS 2004

Причина блокировки - Авто ЗАЗ-DAEWOO LANOS з номером AA0173TE не відповідає вимогам сервера
```

**Что происходит в коде:**
```python
try:
    sleep(5)  # Ждём 5 секунд (чтоб не спамить в телегу)
    bot.send_message(chat_id, text)  # Отправляем в Telegram
except Exception:
    logger.exception("Ошибка отправки")
```

---

### ЭТАП 6️⃣: Сохранение в БД (чтоб не отправить дважды)

Чтоб не отправлять одно и то же уведомление каждый час, приложение **запоминает**, что оно уже отправило:

```
SQLite база: processed_cars.db
Таблица: processed_cars

| ID | TAXI | CARNUM    |
|----|------|-----------|
| 1  | Fly  | AA8732CC  |  ← Эту машину уже сообщали
| 2  | Jet  | KA9628HA  |  ← И эту тоже
| 3  | Fly  | AI3955IE  |  ← И эту
```

**Логика:**
```python
if db.check_record(carnum, taxi):  # Уже сообщали?
    continue                        # Да, пропускаем
else:
    send_message(...)               # Нет, отправляем
    db.insert_record(taxi, carnum)  # И запоминаем
```

---

## ⏰ Полный цикл работы (каждый час)

```
00:00 — Старт
  ↓
[Получаем чёрный список от WD] ← Может не получиться (503 ошибка)
  ↓
[Проверяем 5 таксикомпаний] ← Может потеряться соединение с БД
  ↓
[Ищем совпадения и отправляем]
  ↓
[Сохраняем в БД]
  ↓
[Спим 60 минут]
  ↓
01:00 — Начинаем сначала!
```

---

## 🚨 Система алертов (что может сломаться)

### Когда приложение падает?

```
❌ ОШИБКА #1: WD сервер недоступен
├─ Причина: WD сервер упал или перегружен
├─ Признак: HTTP 503 (Service Unavailable)
└─ Решение: Пробуем через прокси
   Если не помогло → Телегу отправляем админу! 🚨

❌ ОШИБКА #2: Не можем подключиться к БД Firebird
├─ Причина: Сервер Firebird недоступен или БД повреждена
├─ Признак: "Database file not found" или "Network timeout"
└─ Решение: Пробуем переподключиться
   Если 3 раза не вышло → Алерт в Телеграм! 🚨

❌ ОШИБКА #3: Telegram не отвечает
├─ Причина: Проблемы с интернетом или Telegram API
├─ Признак: Не получаем ответ от Telegram
└─ Решение: Пробуем ещё раз через 5 минут
   Если совсем не вышло → Пишем в Sentry! 📊

❌ ОШИБКА #4: Неизвестная ошибка
├─ Причина: Что-то непредвиденное
└─ Решение: Логируем в Sentry и отправляем в Телеграм! 🚨
```

---

## 🛡️ Защита от блокировок (как не поймать бан)

### Проблема:
```
WD может подумать: "Это бот! Он отправляет запросы каждый час!"
И вернуть 503 (Service Unavailable) - "Ты забанен!"
```

### Решение:

**1. Скрываемся под браузер** 🥸
```python
headers = {
    'User-Agent': 'Mozilla/5.0 ... Chrome/120.0 ...'  # Говорим, что это Chrome
    'Accept': 'text/html,application/xhtml+xml,...'
    'Accept-Language': 'uk-UA,uk;q=0.9,en;q=0.8'
}
# WD думает: "Нормальный браузер, не бот!"
```

**2. Добавляем паузы между запросами** ⏸️
```python
REQUEST_DELAY = 2  # Ждём 2 секунды между запросами
sleep(2)           # Имитируем человека, который медленно кликает
```

**3. Используем прокси когда 503** 🔄
```python
if response.status_code == 503:
    # Пробуем через другой IP адрес (прокси)
    response = make_request(url, use_proxy=True)
    # WD видит запрос с другого IP - не блокирует!
```

**4. Переподключаемся каждые 6 часов** 🔌
```python
if session_update_count >= 6:  # Каждые 6 часов
    session = get_session(login, password)  # Создаём новую сессию
    # Это как выйти из аккаунта и зайти заново
```

---

## 📊 Мониторинг ошибок (Sentry)

Когда что-то сломалось, приложение **отправляет информацию в Sentry**:

```
Sentry - это как видеокамера, которая записывает все ошибки.
Потом вы можете посмотреть на сайте Sentry.io:
- Что именно сломалось
- Когда это случилось
- Сколько раз это случилось
- Какая строка кода виновата
```

**Пример в коде:**
```python
try:
    cars = get_cardata(host, database)
except Exception as e:
    sentry_sdk.capture_exception(e)  # ← Отправляем ошибку в Sentry
    logger.exception(e)              # ← Пишем в лог
    send_error_notification(...)     # ← Отправляем в Телеграм
```

---

## 💬 Telegram уведомления (два канала)

### Канал 1️⃣: Основной бот (TELEGRAM_BOT_TOKEN)
```
Сюда приходят сообщения о заблокированных машинах:
"Машина AA8732CC найдена в Fly, водитель Иванов, звоните 0977353647"
```

### Канал 2️⃣: Бот ошибок (TELEGRAM_ERROR_BOT_TOKEN)
```
Сюда приходят алерты о проблемах:
"⚠️ WARNING: WD вернул 503, пробуем через прокси"
"🔴 ERROR: Не можем подключиться к БД Firebird"
"🚨 CRITICAL: 3 ошибки подряд - что-то серьёзное!"
```

---

## 🔐 Где хранятся пароли и токены?

```
❌ БЫЛО (небезопасно):
main.py: token = '5005136355:AAE8e8rNV71_7d1MXuNw4eR3GWY2xgjWmr8'
         login = 'fly'
         password = '0933137532'
         ↓ Если кто-то украдёт код из GitHub - украдёт всё!

✅ СТАЛО (безопасно):
.env файл:
TELEGRAM_BOT_TOKEN=5005136355:AAE8e8rNV71_7d1MXuNw4eR3GWY2xgjWmr8
WD_LOGIN=fly
WD_PASSWORD=0933137532

main.py:
TELEGRAM_BOT_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')
        ↓
Код не содержит пароли! Они только в .env файле!
.env добавлен в .gitignore - не коммитится в GitHub!
```

---

## 📱 Время работы (рабочие часы)

```
check_work_time() проверяет:
"Сейчас в работе? Между 09:10 и 20:30?"

if now > start (09:10) and now < finish (20:30):
    return False  # Работаем!
else:
    return True   # Не работаем, спим
```

**Зачем это нужно?**
- ночью уведомления не отправляем (все спят)
- экономим ресурсы сервера
- не беспокоим диспетчеров в 3 часа ночи

---

## 📈 Как масштабируется

### Сейчас (в одном приложении):
```
1 Приложение → Проверяет 5 таксикомпаний каждый час
```

### В будущем (если хотим оптимизировать):
```
Вариант 1: Параллельные запросы
1 Приложение → Проверяет 5 таксикомпаний ОДНОВРЕМЕННО (async/await)
Плюс: Работает в 5 раз быстрее!

Вариант 2: Несколько приложений
Приложение 1 → Fly, Jet
Приложение 2 → Magdack, 898, Allo
Плюс: Если одно упало - другое работает!

Вариант 3: Кэширование
Если WD не отвечает - используем вчерашний чёрный список
Плюс: Продолжаем работать даже если WD упал!
```

---

## 🎯 Краткое резюме

```
ВСЕ ЧТО ДЕЛАЕТ ПРИЛОЖЕНИЕ:

1. Каждый час в рабочее время (09:10-20:30):
   ├─ Получит список заблокированных машин от WD
   ├─ Для каждой таксикомпании:
   │  ├─ Подключается к БД
   │  ├─ Получает список машин
   │  ├─ Ищет совпадения с чёрным списком
   │  ├─ Если нашёл и не отправляли раньше:
   │  │  ├─ Собирает информацию о машине
   │  │  ├─ Проверяет в полиции
   │  │  ├─ Отправляет в Telegram
   │  │  └─ Сохраняет в БД (чтоб не отправить дважды)
   │  └─ Логирует в файл
   └─ Если что-то упало:
      ├─ Отправляет алерт в Telegram (TELEGRAM_ERROR_BOT_TOKEN)
      └─ Логирует в Sentry (для отслеживания)

2. Защиты от блокировок:
   ├─ Браузер-подобные заголовки (не видны как бот)
   ├─ Паузы между запросами (имитируем человека)
   ├─ Прокси (переключаемся на другой IP)
   └─ Переподключение (обновляем сессию каждые 6 часов)

3. Безопасность:
   ├─ Все пароли в .env файле (не в коде)
   ├─ .env не коммитится в GitHub
   └─ Два разных Telegram бота (для уведомлений и ошибок)
```

---

## ❓ Часто задаваемые вопросы

### Q: Почему приложение может упасть?
**A:** Обычно из-за:
- WD сервер упал или перегружен
- Интернет потеряется
- БД Firebird недоступна
- Telegram не отвечает
- Какой-то неожиданный баг

Приложение справляется с первыми 4, логирует 5-й и уведомляет администратора.

### Q: Что будет если WD скажет 503?
**A:** Приложение:
1. Заметит 503 (Service Unavailable)
2. Отправит WARNING в Telegram
3. Попробует через прокси
4. Если прокси помог - норм, продолжаем
5. Если прокси не помог - отправим ERROR в Telegram и пропустим эту итерацию

### Q: Можно ли отправить сообщение вручную?
**A:** Да, через Python:
```python
import main
main.send_message("Привет! Это тестовое сообщение", -1002045607452)
```

### Q: Что если машину заблокировали вчера, но сегодня разблокировали?
**A:** Машина останется в `processed_cars.db` со статусом "уже сообщали", поэтому уведомление не отправится. Это правильно - нечего спамить!

### Q: Можно ли перезагрузить приложение?
**A:** Да:
- Нажать Ctrl+C (graceful shutdown)
- Потом запустить заново: `python main.py`

### Q: Где смотреть ошибки?
**A:** В трёх местах:
1. **Лог файлы**: `Fly.log`, `Jet.log` и т.д. (в папке проекта)
2. **Sentry**: https://sentry.io (если настроен)
3. **Telegram канал ошибок**: TELEGRAM_ERROR_CHAT_ID
